server:
  host: "0.0.0.0"
  port: 8080

database:
  host: "127.0.0.1"
  port: 5432
  user: "postgres"
  password: "postgres"
  dbname: "log_tools"
  sslmode: "disable"

storage:
  upload_dir: "./data/uploads"
  max_file_size: 524288000  # 500MB

ingest:
  batch_size: 500
  worker_count: 4
