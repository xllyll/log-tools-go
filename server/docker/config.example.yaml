# 【重要】Docker 只挂载 config/config.yaml，不会读本文件！
# 修改本文件后必须执行：
#   cp config.example.yaml config/config.yaml
# 验证容器内配置：
#   docker exec log-tools-server cat /app/config/config.yaml | grep -A6 '^database'

server:
  host: "0.0.0.0"
  port: 8080

database:
  host: "10.110.4.25"          # 外部 PostgreSQL 地址（容器需能访问）
  port: 5432
  user: "postgres"
  password: "xXipX2ndtNabbFJ4"
  dbname: "dev_logs"
  sslmode: "disable"

storage:
  upload_dir: "/app/data/uploads"
  max_file_size: 524288000
  retention_days: 30
  cleanup_interval_hours: 24

ingest:
  batch_size: 500
  worker_count: 5

jira:
  enabled: true
  base_url: "http://120.79.4.206:8080"
  email: "lidaming"
  api_token: "bwlidaming789"
